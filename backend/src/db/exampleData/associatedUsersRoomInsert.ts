import User from "../models/user";
import Room from "../models/room";
import AssociatedUserRoom from "../models/associatedUsersRoom";

const associatedUsersRoomInsert = async () => {
  /*  const myUser = await User.create({
    });

    const guestUser = await User.create({
    });

    const myRoom = await Room.create({
        owner: await myUser.get("id")
    });

    const myRoom2 = await Room.create({
        owner: await myUser.get("id")
    });

    const addGuestToRoom = await AssociatedUserRoom.create({
        roomID: await myRoom.get("id"),
        userID: await guestUser.get("id")
    });
*/
};

export { associatedUsersRoomInsert };
